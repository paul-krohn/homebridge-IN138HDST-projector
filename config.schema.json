{
  "pluginAlias": "IN138HDST Projector",
  "pluginType": "accessory",
  "schema": {
    "name": {
      "title": "Name",
      "type": "string",
      "required": true,
      "default": "Projector"
    },
    "ipAddress": {
      "title": "IP Address",
      "type": "string",
      "required": true,
      "default": "192.168.1.57"
    },
    "timeout": {
      "title": "Timeout (seconds)",
      "type": "integer",
      "required": false,
      "default": 15,
      "description": "Telnet response timeout in seconds. The projector can take ~5s to reply."
    },
    "refreshInterval": {
      "title": "Refresh Interval (seconds)",
      "type": "integer",
      "required": false,
      "default": 15,
      "description": "How often to poll the projector for power state. Polling is automatically suppressed during power-on warm-up (60 s) and power-off cooldown (5 min)."
    },
    "debug": {
      "title": "Debug Logging",
      "type": "boolean",
      "required": false,
      "default": false
    }
  }
}
